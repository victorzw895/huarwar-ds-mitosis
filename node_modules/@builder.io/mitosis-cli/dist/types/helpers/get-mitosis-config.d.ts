import { MitosisConfig } from '@builder.io/mitosis';
/**
 * @param relPath { string } the relative path from pwd to config-file
 */
export declare function getMitosisConfig(relPath?: string): MitosisConfig | null;
