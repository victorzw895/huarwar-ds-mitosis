import { Target } from '@builder.io/mitosis';
export declare const getOverrideFile: ({ path, filename, target, }: {
    path: string;
    filename: string;
    target: Target;
}) => Promise<string | null>;
