/**
 * get all files that match `files` glob, and filter out those that match `exclude` glob.
 *
 */
export declare const getFiles: ({ files, exclude, }: {
    files: string | string[] | undefined;
    exclude: string[] | undefined;
}) => string[];
